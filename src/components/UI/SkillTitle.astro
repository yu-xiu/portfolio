---
type Props = {
    title: string;
    color?: "primary" | "secondary";
};
const { title }: Props = Astro.props;
---

<div class="Fade_Up relative flex_center">
    <p class="text-[1.25em] tracking-tight opacity-90">{title}</p>
</div>
