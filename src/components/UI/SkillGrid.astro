---
import { Image } from "astro:assets";

type Props = {
    stack: {
        name: string;
        icon: ImageMetadata;
        link: string;
    }[];
};

const { stack }: Props = Astro.props;
---

<div class="Fade_Up grid grid-cols-3 md:grid-cols-4 lg:grid-cols-6 gap-x-4 gap-y-8">
    {
        stack.map((obj) => (
            <a
                href={obj.link}
                title={obj.name}
                class="Fade_Stagger flex_center flex-col gap-2 py-2 opacity-80 hover:opacity-100 transition-opacity"
                target="_blank"
                rel="noreferrer"
            >
                <div class="flex_center min-h-[35px] aspect-square">
                    <Image
                        src={obj.icon}
                        alt={obj.name.replaceAll(" ", "_") + "_Icon"}
                        width={35}
                        height={35}
                    />
                </div>
                <span class="text-[0.9em]">{obj.name}</span>
            </a>
        ))
    }
</div>
