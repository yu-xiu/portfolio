---
type Props = {
    title: string;
    color?: "primary" | "secondary";
};
const { title }: Props = Astro.props;
---

<div class="Fade_Up flex_center text-[1.05em] tracking-wide opacity-70 mt-6">
    <p>{title}</p>
</div>
