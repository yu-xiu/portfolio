---
type Props = {
    title: string;
    color?: "primary" | "secondary";
};
const { title, color }: Props = Astro.props;
---

<div
    class={`Fade_Up Title-Tag Title_Line relative flex_center text-[1.5em] ${color}`}
>
    
    <p>{title}</p>
    
</div>
